# Python
__pycache__/
*.py[cod]
.ipynb_checkpoints/

# LaTeX build artifacts
*.aux
*.log
*.out
*.bbl
*.blg
*.synctex.gz
*.toc
report/pg-*.png

# Secrets — never commit the API key
semanticapi.txt

# Provided IEEE template archives (reference only; cls/bst live in report/)
paper/

# Raw Semantic Scholar API dumps (provenance only; curated output in research/citations.*)
research/raw/

# Delivery bundle for Google Drive (duplicate copies of final artifacts)
delivery/

# OS
Thumbs.db
.DS_Store
